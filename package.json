{
  "name": "tideline",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "pnpm --filter api dev",
    "build": "pnpm --filter api build",
    "docker:up": "docker-compose up --build",
    "docker:redis": "docker-compose up redis-manager redis-streams",
    "docker:down": "docker-compose down"
  },
  "pnpm": {
    "allowedBuildScripts": ["esbuild"]
  }
}
